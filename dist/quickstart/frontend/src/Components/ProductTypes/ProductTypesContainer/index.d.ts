import React from "react";
interface Props {
    children?: React.ReactNode | Array<React.ReactNode>;
    productType: string;
}
declare const TypeContainer: React.FC<Props>;
export default TypeContainer;
